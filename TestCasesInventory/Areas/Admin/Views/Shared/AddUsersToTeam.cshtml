@using TestCasesInventory.Presenter.Models
@using PagedList;
@using PagedList.Mvc;
@using TestCasesInventory.Web.Common.Utils;

@model  IPagedList<UsersNotBelongTeamViewModel>

@Html.Action("FilterForAddUserToTeam", "Filter", new { area = "" })
<form action="~/Admin/Team/AddUsersToTeam/@ViewBag.TeamID" method="post">
    <table class="table">
        <tr>
            <th>Email</th>
            <th>Display Name</th>
            <th>Current Team</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <input type="checkbox" id=@item.ID name="usersToAdd" value="@item.ID" />
                    <label for="@item.ID"> @item.Email</label>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DisplayName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TeamName)
                </td>
            </tr>
        }
    </table>
@Html.PagedListPager(Model, page => Request.AddQueryString(new KeyValuePair<string, string>("p", page.ToString())))
    <input type="submit" value="Add to Team" name="buttonSubmitIn" class="btn btn-default" />
</form>
